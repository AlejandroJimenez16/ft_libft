#ft_is***
-----------

#include <stdio.h>
#include <ctype.h>

int main(void)
{
    int c = 'a';
    
    printf("Original: %d\n", isalpha(c));
    printf("Copia: %d\n", ft_isalpha(c));
}

#ft_strlen
-----------

#include <string.h>
#include <stdio.h>

int main(void)
{
	char *str = "holaa";

	printf("Original: %ld\n", strlen(str));
    printf("Copia: %ld\n", ft_strlen(str));
}

#ft_memset
-----------

#include <string.h>
#include <stdio.h>

int main(void)
{
    char buffer[10];
    size_t i;

    ft_memset(buffer, 'c', sizeof(buffer));

    i = 0;
    while(i < sizeof(buffer))
    {
        printf("%c", buffer[i]);
        i++;
    }
}

#ft_bzero
-----------

#include <strings.h>
#include <stdio.h>

int main(void)
{
    char buffer[5];
    size_t i;

    ft_bzero(buffer, sizeof(buffer));

    i = 0;
    while (i < sizeof(buffer))
    {
        printf("%d", buffer[i]);
        i++;
    }
}